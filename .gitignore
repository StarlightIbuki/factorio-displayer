*.pyc
__pycache__/
*.egg-info

*.pkl
dist
_diagnose


.encode_chunks*
.encode_vchunks*
.blueprint_cache
.factorio_display_cache
server_data
server_data/
tmp_*
tmp-*
.venv*

# Generated eval output (WAVs, blueprints, reports) — reproduced by the
# eval scripts in eval_midi/ (make_reference_midis.py, eval_real_track.py,
# eval_fidelity.py, render_drums.py).
eval_midi/out/
eval_midi/ref_midis/

# CLI output redirects / large generated artifacts (can be hundreds of MB
# of blueprint string — keeps the VS Code git GUI responsive).
out.txt